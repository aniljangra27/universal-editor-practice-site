// export default function decorate(block) {
//   const quoteWrapper = block.querySelector('[data-name="quote"]');
//   if (!quoteWrapper) return;

//   const blockquote = document.createElement('blockquote');

//   // Move existing nodes instead of recreating them
//   while (quoteWrapper.firstChild) {
//     blockquote.appendChild(quoteWrapper.firstChild);
//   }

//   quoteWrapper.appendChild(blockquote);
// }

